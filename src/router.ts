/**
 * Re-exports the `AutoRouter` from the `itty-router` package as `Router`.
 * This provides a simplified way to access the router when using this library.
 *
 * For detailed usage and additional features of `itty-router`, refer to its documentation:
 * https://github.com/kwhitley/itty-router
 */
import { AutoRouter as Router } from 'itty-router';

export { Router };
